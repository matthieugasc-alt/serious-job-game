# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Données mutables à l'exécution (NE JAMAIS suivre : un `git pull` au
# déploiement écraserait/entrerait en conflit avec les données users).
# Les seeds (founder_rules, founder_invariants, categories, competencies)
# restent suivis.
/data/users.json
/data/sessions.json
/data/founder_campaigns/
/data/game_records/
/data/v2_completions/
